
<nav class="navbar navbar-expand-lg navbar-light bg-info" id="app-navbar">
    <div class="container-fluid">
        <ul class="navbar-nav">
            <li class="navbar-brand">
                <img src="../../static/images/icon.png" style="height:25px;width:25px;">
            </li>
            <li class="nav-item">
                <a class="nav-link active" aria-current="page" href="{{url_for('main.index')}}">Believable Bridges</a>
            </li>
            <li class="nav-item">
                <a class="nav-link active" aria-current="page" href="{{url_for('main.listings', id=0)}}">Listings</a>
            </li>
        </ul>
        <ul class="navbar-nav ms-auto align items-center">
            <li class="nav-item">
                 <a class="nav-link active" aria-current="page" href="{{url_for('main.cart')}}">
                <div >
                    <img src="../../static/images/cart4.svg" alt="Bootstrap" width="20" height="20">
                <span class="position-absolute top-5 start-80 translate-middle badge rounded-pill bg-danger">
                    {% if current_user.items %}
                    {{ current_user.items.count() }}
                    {% else %}
                    0
                    {% endif %}
                </span>
                </div>
                </a>
            </li>
            {% if current_user.is_authenticated %}
            <li class="nav-item">
                <a class="nav-link active" href="{{url_for('auth.edit_profile')}}">Edit Profile</a>
            </li>
            <li class="nav-item">
                <a class="nav-link active" href="{{url_for('auth.logout')}}">Logout</a>
            </li>
            {% else %}
            <li class="nav-item">
                <a class="nav-link active" href="{{url_for('auth.login')}}">Login</a>
            </li>
            {% endif %}
        </ul>
    </div>
</nav>
